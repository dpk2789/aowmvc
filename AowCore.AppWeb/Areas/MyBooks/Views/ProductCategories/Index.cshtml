@model string

@{
    ViewData["Title"] = "Product Categories";
    Layout = "~/Areas/MyBooks/Views/Shared/_Layout.cshtml";
}


<div class="box-inner">
    <div class="box-content">
        @*<h2>Product Category</h2>
            <hr />*@
        <p class="text-danger">@TempData["MessageToClient"]</p>
        <p class="text-success">@TempData["MessageToClientSuccess"]</p>
        <div class="panel-heading">
            <span class="panel-title"><i class="fa fa-2x fa-sitemap"></i><strong>Item Category </strong> </span>
            <span class="pull-right">
                <a href="/MyBooks/ProductCategories/Create" class="btn btn-sm btn-success">
                    <span class="btn-label icon fa fa-plus"></span>Add Item Category
                </a>
            </span>
        </div>
        <div class="table-responsive">
            @*<table class="table table-hover table-striped table-condensed table-bordered" id="dataTables">
                    <p>
                        <br />
                        <a href="/Books/ProductCategories/Create" class="btn btn-success btn-xs">Create New</a>
                    </p>
                </table>*@
            <div id="categories">
                @Html.Raw(@Model)
            </div>
        </div>
    </div>
</div>

@*<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ParentCategoryId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Company)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ParentCategoryId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Company.Id)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>*@
